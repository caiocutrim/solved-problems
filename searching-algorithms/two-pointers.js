


function pairs(k, arr) {
  arr = arr.sort((a, b) => a - b)
  let count = 0
  const size = arr.length
  let i = 0
  let j = size - 1 
  while(i < j) {
    const key = arr[i]
    const lastKey = arr[j]
  }
}